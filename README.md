**To run: (in terminal)**
------------------------------------------------------------------
- must upload Task Status Excel
- must create .env file with OpenAI API key
------------------------------------------------------------------
- py -m venv venv (to create for the first time)
- .\venv\Scripts\Activate.ps1
- pip install -r requirements.txt (for the first time)
- py -m streamlit run app.py


